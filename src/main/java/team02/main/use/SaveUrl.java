package team02.main.use;

import java.util.Stack;

public class SaveUrl {
        public Stack<String> saveUrltoStack(String url, int action) {
        	Stack<String> saveUrlStack = new Stack<String>();
        	if(saveUrlStack.isEmpty()) {
        		
        	}
        	
        	
        	return saveUrlStack;
        }
  
}
